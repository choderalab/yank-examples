package:
  name: yank-examples
  version: !!str 0.0.0

source:
  path: ../..

requirements:
  build:
    - yank >=0.12.0

  run:
    - yank >=0.12.0

test:
  requires:
    - nose
    - nose-timer
  imports:
    - yank

about:
  home: https://github.com/choderalab/yank-examples
  license: MIT License
